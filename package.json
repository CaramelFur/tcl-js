{
  "name": "tcl-js",
  "version": "3.0.0",
  "description": "tcl-js is a tcl intepreter written completely in Typescript. It's meant to replicate the tcl-sh interpreter as closely as possible.",
  "main": "dist/tcl.js",
  "scripts": {
    "run": "yarn run generate-pegjs && ts-node ./test/test.ts",
    "test": "yarn run run-tests && yarn run report-coverage",
    "build": "yarn run make-prettier && yarn run generate-pegjs && yarn run compile-typescript && yarn run generate-docs",
    "generate-pegjs": "ts-node ./src/pegjs/generate-pegjs.ts",
    "make-prettier": "prettier --write ./src/ ./test/ | tr -d '\\033'",
    "compile-typescript": "rm -r dist && tsc",
    "generate-docs": "typedoc --options ./typedoc.json ./src/",
    "run-tests": "nyc mocha -r ts-node/register test/index.ts",
    "report-coverage": "mkdir -p ./coverage/ && nyc report --reporter=text-lcov > coverage/coverage.lcov && codecov --disable=gcov",
    "start": "echo Error: There is nothing to start && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rubikscraft/tcl-js.git"
  },
  "license": "BSD-2-Clause",
  "keywords": [
    "tcl",
    "tcl/tk",
    "tcl-sh",
    "javascript",
    "interpreter",
    "language",
    "parser",
    "typescript",
    "script",
    "lexer"
  ],
  "author": "Rubikscraft <contact@rubikscraft.nl> (https://rubikscraft.nl)",
  "bugs": {
    "url": "https://github.com/rubikscraft/tcl-js/issues"
  },
  "homepage": "https://github.com/rubikscraft/tcl-js#readme",
  "devDependencies": {
    "@invisible/pegjs-import": "^1.1.1",
    "@istanbuljs/nyc-config-typescript": "^1.0.1",
    "@types/chai": "^4.2.14",
    "@types/mocha": "^8.0.3",
    "@types/node": "^14.14.0",
    "@types/pegjs": "^0.10.2",
    "@typescript-eslint/eslint-plugin": "^4.5.0",
    "@typescript-eslint/parser": "^4.5.0",
    "chai": "^4.2.0",
    "codecov": "^3.8.0",
    "eslint": "^7.11.0",
    "eslint-config-airbnb-typescript": "^12.0.0",
    "mocha": "^8.2.0",
    "nyc": "^15.1.0",
    "pegjs": "^0.10.0",
    "prettier": "^2.1.2",
    "prettier-plugin-pegjs": "^0.3.1",
    "ts-node": "^9.0.0",
    "ts-pegjs": "^0.2.7",
    "typedoc": "^0.19.2",
    "typescript": "^4.0.3"
  },
  "dependencies": {}
}
